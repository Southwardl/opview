name: opview
description: openpilot onroad UI over WebRTC
publish_to: 'none'
version: 0.1.1

environment:
  sdk: ^3.2.0

dependencies:
  flutter:
    sdk: flutter
  flutter_webrtc: 0.9.48+hotfix.1
  bonsoir: ^5.1.11
  http: ^1.2.0
  connectivity_plus: ^5.0.2
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
